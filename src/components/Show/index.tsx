import React, { Component, Fragment } from 'react';

class Show extends Component {
    render() {
        return (
            <Fragment>
                Show
            </Fragment>
        );
    }
}

export default Show;
